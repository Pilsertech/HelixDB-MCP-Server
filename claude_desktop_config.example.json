{
  "mcpServers": {
    "ai-memory-stdio": {
      "command": "helix-mcp-server.exe",
      "env": {
        "HELIX_TRANSPORT": "stdio"
      }
    },
    "ai-memory-tcp": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/client",
        "tcp://localhost:8765"
      ]
    },
    "ai-memory-http": {
      "url": "http://localhost:8766"
    }
  },
  "comments": {
    "ai-memory-stdio": "Direct STDIO transport - fastest, recommended for local use",
    "ai-memory-tcp": "TCP transport on port 8765 - for remote connections",
    "ai-memory-http": "HTTP transport on port 8766 - for web/REST API access with full tool support",
    "note": "Both TCP and HTTP servers run simultaneously when enabled in mcpconfig.toml"
  }
}
